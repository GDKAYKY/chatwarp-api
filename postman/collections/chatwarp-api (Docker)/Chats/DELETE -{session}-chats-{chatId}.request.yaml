$kind: http-request
name: DELETE /{session}/chats/{chatId}
url: "{{base_url}}/:session/chats/:chatId"
method: DELETE
scripts:
  - type: afterResponse
    code: pm.test('status is not 500', function () {
      pm.response.to.not.have.status(500); });
    language: text/javascript
order: 4000
