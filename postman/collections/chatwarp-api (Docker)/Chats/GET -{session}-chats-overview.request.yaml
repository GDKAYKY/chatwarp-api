$kind: http-request
name: GET /{session}/chats/overview
url: "{{base_url}}/:session/chats/overview"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('status is not 500', function () {
      pm.response.to.not.have.status(500); });
    language: text/javascript
order: 2000
