$kind: http-request
name: PUT /{session}/chats/{chatId}/messages/{messageId}
url: "{{base_url}}/:session/chats/:chatId/messages/:messageId"
method: PUT
scripts:
  - type: afterResponse
    code: pm.test('status is not 500', function () {
      pm.response.to.not.have.status(500); });
    language: text/javascript
order: 11000
