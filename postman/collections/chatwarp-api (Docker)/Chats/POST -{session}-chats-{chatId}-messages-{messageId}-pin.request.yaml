$kind: http-request
name: POST /{session}/chats/{chatId}/messages/{messageId}/pin
url: "{{base_url}}/:session/chats/:chatId/messages/:messageId/pin"
method: POST
scripts:
  - type: afterResponse
    code: pm.test('status is not 500', function () {
      pm.response.to.not.have.status(500); });
    language: text/javascript
order: 12000
