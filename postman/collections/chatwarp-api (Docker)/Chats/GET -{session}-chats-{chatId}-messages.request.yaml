$kind: http-request
name: GET /{session}/chats/{chatId}/messages
url: "{{base_url}}/:session/chats/:chatId/messages"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('status is not 500', function () {
      pm.response.to.not.have.status(500); });
    language: text/javascript
order: 6000
