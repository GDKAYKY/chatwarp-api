$kind: http-request
name: POST /{session}/media/convert/voice
url: "{{base_url}}/:session/media/convert/voice"
method: POST
scripts:
  - type: afterResponse
    code: pm.test('status is not 500', function () {
      pm.response.to.not.have.status(500); });
    language: text/javascript
order: 1000
