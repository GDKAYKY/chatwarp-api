$kind: http-request
name: POST /{session}/media/convert/video
url: "{{base_url}}/:session/media/convert/video"
method: POST
scripts:
  - type: afterResponse
    code: pm.test('status is not 500', function () {
      pm.response.to.not.have.status(500); });
    language: text/javascript
order: 2000
