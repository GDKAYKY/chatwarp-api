$kind: http-request
name: POST /sessions/{session}/stop
url: "{{base_url}}/sessions/:session/stop"
method: POST
scripts:
  - type: afterResponse
    code: pm.test('status is not 500', function () {
      pm.response.to.not.have.status(500); });
    language: text/javascript
order: 8000
