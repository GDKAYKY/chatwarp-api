$kind: http-request
name: POST /sessions/stop
url: "{{base_url}}/sessions/stop"
method: POST
scripts:
  - type: afterResponse
    code: pm.test('status is not 500', function () {
      pm.response.to.not.have.status(500); });
    language: text/javascript
order: 12000
