$kind: http-request
name: GET /sessions/{session}/me
url: "{{base_url}}/sessions/:session/me"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('status is not 500', function () {
      pm.response.to.not.have.status(500); });
    language: text/javascript
order: 6000
