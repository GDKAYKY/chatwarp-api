$kind: http-request
name: GET /sessions/{session}
url: "{{base_url}}/sessions/{{instance_name}}"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('status is not 500', function () {
      pm.response.to.not.have.status(500); });
    language: text/javascript
order: 3000
