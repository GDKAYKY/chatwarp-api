$kind: http-request
name: POST /sessions/{session}/logout
url: "{{base_url}}/sessions/:session/logout"
method: POST
scripts:
  - type: afterResponse
    code: pm.test('status is not 500', function () {
      pm.response.to.not.have.status(500); });
    language: text/javascript
order: 9000
