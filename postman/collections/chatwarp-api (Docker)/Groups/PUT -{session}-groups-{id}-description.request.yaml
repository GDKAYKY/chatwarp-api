$kind: http-request
name: PUT /{session}/groups/{id}/description
url: "{{base_url}}/:session/groups/:id/description"
method: PUT
scripts:
  - type: afterResponse
    code: pm.test('status is not 500', function () {
      pm.response.to.not.have.status(500); });
    language: text/javascript
order: 13000
