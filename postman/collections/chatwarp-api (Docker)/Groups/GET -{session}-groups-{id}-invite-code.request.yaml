$kind: http-request
name: GET /{session}/groups/{id}/invite-code
url: "{{base_url}}/:session/groups/:id/invite-code"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('status is not 500', function () {
      pm.response.to.not.have.status(500); });
    language: text/javascript
order: 19000
