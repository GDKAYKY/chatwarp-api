$kind: http-request
name: POST /{session}/groups/{id}/leave
url: "{{base_url}}/:session/groups/:id/leave"
method: POST
scripts:
  - type: afterResponse
    code: pm.test('status is not 500', function () {
      pm.response.to.not.have.status(500); });
    language: text/javascript
order: 9000
