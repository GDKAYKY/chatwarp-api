$kind: http-request
name: GET /{session}/groups/{id}/participants/v2
url: "{{base_url}}/:session/groups/:id/participants/v2"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('status is not 500', function () {
      pm.response.to.not.have.status(500); });
    language: text/javascript
order: 22000
