$kind: http-request
name: POST /{session}/groups
url: "{{base_url}}/:session/groups"
method: POST
scripts:
  - type: afterResponse
    code: pm.test('status is not 500', function () {
      pm.response.to.not.have.status(500); });
    language: text/javascript
order: 1000
