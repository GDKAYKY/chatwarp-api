$kind: http-request
name: PUT /{session}/groups/{id}/settings/security/info-admin-only
url: "{{base_url}}/:session/groups/:id/settings/security/info-admin-only"
method: PUT
scripts:
  - type: afterResponse
    code: pm.test('status is not 500', function () {
      pm.response.to.not.have.status(500); });
    language: text/javascript
order: 15000
