$kind: http-request
name: POST /{session}/labels
url: "{{base_url}}/:session/labels"
method: POST
scripts:
  - type: afterResponse
    code: pm.test('status is not 500', function () {
      pm.response.to.not.have.status(500); });
    language: text/javascript
order: 2000
