$kind: http-request
name: PUT /{session}/labels/chats/{chatId}
url: "{{base_url}}/:session/labels/chats/:chatId"
method: PUT
scripts:
  - type: afterResponse
    code: pm.test('status is not 500', function () {
      pm.response.to.not.have.status(500); });
    language: text/javascript
order: 6000
