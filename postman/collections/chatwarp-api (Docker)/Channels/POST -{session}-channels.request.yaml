$kind: http-request
name: POST /{session}/channels
url: "{{base_url}}/:session/channels"
method: POST
scripts:
  - type: afterResponse
    code: pm.test('status is not 500', function () {
      pm.response.to.not.have.status(500); });
    language: text/javascript
order: 2000
