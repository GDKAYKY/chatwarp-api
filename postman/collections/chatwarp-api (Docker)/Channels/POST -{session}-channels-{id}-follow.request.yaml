$kind: http-request
name: POST /{session}/channels/{id}/follow
url: "{{base_url}}/:session/channels/:id/follow"
method: POST
scripts:
  - type: afterResponse
    code: pm.test('status is not 500', function () {
      pm.response.to.not.have.status(500); });
    language: text/javascript
order: 6000
