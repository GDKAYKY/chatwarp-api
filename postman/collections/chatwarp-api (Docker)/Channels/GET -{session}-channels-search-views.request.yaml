$kind: http-request
name: GET /{session}/channels/search/views
url: "{{base_url}}/:session/channels/search/views"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('status is not 500', function () {
      pm.response.to.not.have.status(500); });
    language: text/javascript
order: 12000
