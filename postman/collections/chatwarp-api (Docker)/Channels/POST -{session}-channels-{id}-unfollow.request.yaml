$kind: http-request
name: POST /{session}/channels/{id}/unfollow
url: "{{base_url}}/:session/channels/:id/unfollow"
method: POST
scripts:
  - type: afterResponse
    code: pm.test('status is not 500', function () {
      pm.response.to.not.have.status(500); });
    language: text/javascript
order: 7000
