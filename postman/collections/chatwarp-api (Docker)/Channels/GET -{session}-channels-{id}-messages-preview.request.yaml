$kind: http-request
name: GET /{session}/channels/{id}/messages/preview
url: "{{base_url}}/:session/channels/:id/messages/preview"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('status is not 500', function () {
      pm.response.to.not.have.status(500); });
    language: text/javascript
order: 5000
