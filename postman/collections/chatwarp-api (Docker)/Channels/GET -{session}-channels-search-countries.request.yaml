$kind: http-request
name: GET /{session}/channels/search/countries
url: "{{base_url}}/:session/channels/search/countries"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('status is not 500', function () {
      pm.response.to.not.have.status(500); });
    language: text/javascript
order: 13000
