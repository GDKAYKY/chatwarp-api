$kind: http-request
name: POST /{session}/channels/search/by-text
url: "{{base_url}}/:session/channels/search/by-text"
method: POST
scripts:
  - type: afterResponse
    code: pm.test('status is not 500', function () {
      pm.response.to.not.have.status(500); });
    language: text/javascript
order: 11000
