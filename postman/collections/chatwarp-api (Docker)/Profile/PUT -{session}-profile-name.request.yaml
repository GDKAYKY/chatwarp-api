$kind: http-request
name: PUT /{session}/profile/name
url: "{{base_url}}/:session/profile/name"
method: PUT
scripts:
  - type: afterResponse
    code: pm.test('status is not 500', function () {
      pm.response.to.not.have.status(500); });
    language: text/javascript
order: 2000
