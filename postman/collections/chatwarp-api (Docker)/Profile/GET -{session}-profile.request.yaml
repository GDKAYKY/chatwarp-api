$kind: http-request
name: GET /{session}/profile
url: "{{base_url}}/:session/profile"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('status is not 500', function () {
      pm.response.to.not.have.status(500); });
    language: text/javascript
order: 1000
