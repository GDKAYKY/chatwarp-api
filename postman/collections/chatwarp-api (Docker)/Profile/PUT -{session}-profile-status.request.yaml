$kind: http-request
name: PUT /{session}/profile/status
url: "{{base_url}}/:session/profile/status"
method: PUT
scripts:
  - type: afterResponse
    code: pm.test('status is not 500', function () {
      pm.response.to.not.have.status(500); });
    language: text/javascript
order: 3000
