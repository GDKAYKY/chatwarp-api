$kind: http-request
name: GET /{session}/lids/pn/{phoneNumber}
url: "{{base_url}}/:session/lids/pn/:phoneNumber"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('status is not 500', function () {
      pm.response.to.not.have.status(500); });
    language: text/javascript
order: 12000
