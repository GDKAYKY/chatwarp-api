$kind: http-request
name: PUT /{session}/contacts/{chatId}
url: "{{base_url}}/:session/contacts/:chatId"
method: PUT
scripts:
  - type: afterResponse
    code: pm.test('status is not 500', function () {
      pm.response.to.not.have.status(500); });
    language: text/javascript
order: 8000
