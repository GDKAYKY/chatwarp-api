$kind: http-request
name: GET /{session}/lids/count
url: "{{base_url}}/:session/lids/count"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('status is not 500', function () {
      pm.response.to.not.have.status(500); });
    language: text/javascript
order: 10000
