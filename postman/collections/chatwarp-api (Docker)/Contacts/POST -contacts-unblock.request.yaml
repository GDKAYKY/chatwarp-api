$kind: http-request
name: POST /contacts/unblock
url: "{{base_url}}/contacts/unblock"
method: POST
scripts:
  - type: afterResponse
    code: pm.test('status is not 500', function () {
      pm.response.to.not.have.status(500); });
    language: text/javascript
order: 7000
