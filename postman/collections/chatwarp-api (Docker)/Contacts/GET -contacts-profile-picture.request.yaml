$kind: http-request
name: GET /contacts/profile-picture
url: "{{base_url}}/contacts/profile-picture"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('status is not 500', function () {
      pm.response.to.not.have.status(500); });
    language: text/javascript
order: 5000
