$kind: http-request
name: GET /{session}/lids/{lid}
url: "{{base_url}}/:session/lids/:lid"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('status is not 500', function () {
      pm.response.to.not.have.status(500); });
    language: text/javascript
order: 11000
