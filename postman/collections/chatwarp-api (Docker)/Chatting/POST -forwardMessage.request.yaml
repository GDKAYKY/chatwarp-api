$kind: http-request
name: POST /forwardMessage
url: "{{base_url}}/forwardMessage"
method: POST
scripts:
  - type: afterResponse
    code: pm.test('status is not 500', function () {
      pm.response.to.not.have.status(500); });
    language: text/javascript
order: 10000
