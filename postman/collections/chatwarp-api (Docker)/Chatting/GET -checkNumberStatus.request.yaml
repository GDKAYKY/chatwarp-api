$kind: http-request
name: GET /checkNumberStatus
url: "{{base_url}}/checkNumberStatus"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('status is not 500', function () {
      pm.response.to.not.have.status(500); });
    language: text/javascript
order: 22000
