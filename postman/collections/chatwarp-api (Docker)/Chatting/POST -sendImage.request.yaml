$kind: http-request
name: POST /sendImage
url: "{{base_url}}/sendImage"
method: POST
scripts:
  - type: afterResponse
    code: pm.test('status is not 500', function () {
      pm.response.to.not.have.status(500); });
    language: text/javascript
order: 3000
