$kind: http-request
name: POST /sendList
url: "{{base_url}}/sendList"
method: POST
scripts:
  - type: afterResponse
    code: pm.test('status is not 500', function () {
      pm.response.to.not.have.status(500); });
    language: text/javascript
order: 9000
