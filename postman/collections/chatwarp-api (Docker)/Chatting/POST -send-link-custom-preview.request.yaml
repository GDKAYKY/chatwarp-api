$kind: http-request
name: POST /send/link-custom-preview
url: "{{base_url}}/send/link-custom-preview"
method: POST
scripts:
  - type: afterResponse
    code: pm.test('status is not 500', function () {
      pm.response.to.not.have.status(500); });
    language: text/javascript
order: 7000
