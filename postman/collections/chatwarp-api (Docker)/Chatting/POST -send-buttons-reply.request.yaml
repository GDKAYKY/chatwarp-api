$kind: http-request
name: POST /send/buttons/reply
url: "{{base_url}}/send/buttons/reply"
method: POST
scripts:
  - type: afterResponse
    code: pm.test('status is not 500', function () {
      pm.response.to.not.have.status(500); });
    language: text/javascript
order: 20000
