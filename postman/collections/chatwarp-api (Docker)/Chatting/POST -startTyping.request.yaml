$kind: http-request
name: POST /startTyping
url: "{{base_url}}/startTyping"
method: POST
scripts:
  - type: afterResponse
    code: pm.test('status is not 500', function () {
      pm.response.to.not.have.status(500); });
    language: text/javascript
order: 12000
