$kind: http-request
name: GET /server/debug/cpu
url: "{{base_url}}/server/debug/cpu"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('status is not 500', function () {
      pm.response.to.not.have.status(500); });
    language: text/javascript
order: 7000
