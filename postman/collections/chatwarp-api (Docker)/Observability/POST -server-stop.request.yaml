$kind: http-request
name: POST /server/stop
url: "{{base_url}}/server/stop"
method: POST
scripts:
  - type: afterResponse
    code: pm.test('status is not 500', function () {
      pm.response.to.not.have.status(500); });
    language: text/javascript
order: 6000
