$kind: http-request
name: GET /server/debug/browser/trace/{session}
url: "{{base_url}}/server/debug/browser/trace/:session"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('status is not 500', function () {
      pm.response.to.not.have.status(500); });
    language: text/javascript
order: 9000
