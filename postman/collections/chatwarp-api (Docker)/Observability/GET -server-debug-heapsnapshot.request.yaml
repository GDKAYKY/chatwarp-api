$kind: http-request
name: GET /server/debug/heapsnapshot
url: "{{base_url}}/server/debug/heapsnapshot"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('status is not 500', function () {
      pm.response.to.not.have.status(500); });
    language: text/javascript
order: 8000
