$kind: http-request
name: GET /docs/swagger
url: "{{base_url}}/docs/swagger"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('status is not 500', function () {
      pm.response.to.not.have.status(500); });
    language: text/javascript
order: 3000
